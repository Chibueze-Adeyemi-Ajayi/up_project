const Footer = () => {
    return (
        <footer className="w-full h-fit p-3 flex">
            <span className="w-fit mx-auto my-auto">Copy &copy; UpFinance {new Date().getFullYear()}</span>
        </footer>
    );
}

export default Footer;